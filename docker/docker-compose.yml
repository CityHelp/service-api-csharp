version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service-api-csharp
    restart: unless-stopped
     
    ports:
      - "2020:2020"

    env_file:
      - ../service-api-csharp.API/.env
    

